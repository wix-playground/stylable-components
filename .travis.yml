dist: trusty
language: node_js

node_js:
  - '8'
  - '6'

sudo: false

addons:
  apt:
    packages:
      - google-chrome-stable

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start &
  - sleep 3

cache:
  yarn: true
